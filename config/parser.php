<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Настройки Telegram
    |--------------------------------------------------------------------------
    */
    'telegram' => [
        'api_id'            => env('TELEGRAM_API_ID'),
        'api_hash'          => env('TELEGRAM_API_HASH'),
        'phone'             => env('TELEGRAM_PHONE'),
        'trade_chat_id'     => env('TELEGRAM_TRADE_CHAT_ID'),
        'epsilon_chat_id'   => env('TELEGRAM_EPSILON_CHAT_ID'),
        'session_path'      => env('MADELINE_SESSION_PATH', 'storage/madeline/session.madeline'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки базы данных MadelineProto
    |--------------------------------------------------------------------------
    */
    'madeline_db' => [
        'host'     => env('MADELINE_DB_HOST', '127.0.0.1'),
        'port'     => env('MADELINE_DB_PORT', 3306),
        'database' => env('MADELINE_DB_DATABASE', 'telegram_session'),
        'username' => env('MADELINE_DB_USERNAME', 'root'),
        'password' => env('MADELINE_DB_PASSWORD', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки загрузки сообщений
    |--------------------------------------------------------------------------
    */
    'fetch' => [
        // Количество дней для загрузки сообщений
        'days' => env('FETCH_DAYS', 30),

        // Количество сообщений за один запрос к Telegram API
        'batch_size' => 100,
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки аномалий цен
    |--------------------------------------------------------------------------
    | Если цена отклоняется от среднего за последние anomaly_days дней
    | более чем на threshold процентов — запись помечается как suspicious.
    |--------------------------------------------------------------------------
    */
    'anomaly' => [
        // Порог отклонения в процентах
        'threshold' => env('PARSER_ANOMALY_THRESHOLD', 50),

        // Количество дней для расчёта среднего
        'days' => env('PARSER_ANOMALY_DAYS', 7),

        // Минимальное количество цен для расчёта среднего
        // (если меньше — аномалию не определяем, слишком мало данных)
        'min_samples' => 3,
    ],

    'matching' => [
        'fuzzy_threshold' => env('PARSER_FUZZY_THRESHOLD', 85),
    ],

    /*
    |--------------------------------------------------------------------------
    | Маппинг тегов на типы объявлений
    |--------------------------------------------------------------------------
    */
    'tag_map' => [
        // Продажа
        'sell' => [
            '#продам', '#продаю', '#продажа', '#sell',
        ],
        // Покупка
        'buy' => [
            '#куплю', '#скупка', '#скуплю', '#скупаю', '#buy',
            '#ищу', '#куплю_безы', '#куплю_рецепты',
        ],
        // Обмен
        'trade' => [
            '#обмен', '#обменяю', '#меняю', '#мен',
        ],
        // Услуги и найм
        'service' => [
            '#услуги', '#услуга', '#крафтер', '#алхимик',
            '#заточки', '#свитки', '#найму', '#найм',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Ключевые слова для определения типа без тегов
    |--------------------------------------------------------------------------
    */
    'keyword_map' => [
        'sell'    => ['продам', 'продаю', 'продается', 'продаётся'],
        'buy'     => ['куплю', 'покупаю', 'скупаю', 'ищу'],
        'trade'   => ['обменяю', 'меняю', 'обмен'],
        'service' => ['предлагаю услуги', 'выполню', 'найму', 'ищу работника'],
    ],

    /*
    |--------------------------------------------------------------------------
    | Валюты
    |--------------------------------------------------------------------------
    */
    'currencies' => [
        'gold'   => '💰',
        'cookie' => '🍪',
    ],
];
